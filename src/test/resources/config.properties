# Application
base.url=http://hms-app:8080

# Credentials
admin.username=admin
admin.password=admin123

# Browser (chrome | firefox | edge)
browser=chrome
headless=false

# Selenium Grid
grid.url=http://selenium-chrome:4444

# Timeouts (seconds)
implicit.wait=10
explicit.wait=30

# API
api.base.url=http://hms-app:8080/api
